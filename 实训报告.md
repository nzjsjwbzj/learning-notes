明确：列表项 任务，列表 ，队列之间的关系

一个任务创建时有两个列表项

一个是用来信号量，队列的，一个是来标记任务状态的，就绪，执行，阻塞，挂起

这两个列表项里都有指向当前任务的TCB指针





有了上面的基础，就知道了要记录任务，记录它的两个分身就是了

创建一个队列，这个队列里面包含了两个列表，一个是等待任务发数据的列表，一个是等待任务收数据的列表

当一个任务调用xQueueReceive(my_queue,max)  在等待数据进入队列时，

