# 迭代器的使用

![image-20240928134001685](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20240928134001685.png)

**a是容器中的元素，不是索引**

![image-20241008192332976](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008192332976.png)

![image-20241006130800053](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006130800053.png)

# dp[i] [j]表示背包容量为j的情况下前i个物品价格最大值，回溯很棒

#include<bits/stdc++.h>
using namespace std;
int main() {
	vector<string> items = { "水", "书", "食物", "夹克", "相机" };
	vector<int>weight = { 3,1,2,2,1 };
	vector<int>value = { 10,3,9,5,6 };
	int W = 6;
	int N = weight.size();
	vector<vector<int>>dp(N + 1, vector<int>(W + 1, 0));
	for (int i = 1; i <= N; i++) {
		for (int j = 1; j <= W; j++) {
			if (j >= weight[i - 1]) {
				dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - weight[i-1]] + value[i-1]);
			}
			else {
				dp[i][j] = dp[i - 1][j];
			}
			if(j==W)
			cout << dp[i][j]<<endl;
			else
				cout << dp[i][j] << " ";
		}
	}
	// 回溯找到被选择的物品
	int w = W;
	vector<string> chosenItems;
	for (int i = N; i > 0; --i) {
		if (dp[i][w] != dp[i - 1][w]) {  // 如果价值不相等，说明选择了该物品
			chosenItems.push_back(items[i - 1]);
			w -= weight[i - 1];  // 减去相应的重量
		}
	}

```c++
// 输出选择的物品
cout << "选择携带的物品有: ";
for (const string& item : chosenItems) {
	cout << item << " ";
}
cout << endl;
return 0;
```
}

# 数学运算要注意

## 1.

![image-20241006170156015](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006170156015.png)



## 2.

使用“/”时前面要乘以1.0，前面是整数结果是整数，前面是小数结果是小数。

## 3.![image-20241006170527475](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006170527475.png)

 double epsilon = 1e-4;  // 误差范围

## 4.

对数运算在编程里的使用格式要了解

# to_string()与子序列，子数组，子串

## 子序列

![image-20241006171339938](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006171339938.png)

![image-20241006171415814](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006171415814.png)

总结：序列可以不连续，其他要连续

## to_string()

将数值类型转换为字符串

![image-20241006171849095](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241006171849095.png)

# sort()总结(algorithm库)

## 1.使用格式

![image-20241008190051653](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008190051653.png)

![image-20241008190142168](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008190142168.png)

## 2.实现降序

![image-20241008190307565](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008190307565.png)

cmp()要在main()外面

![image-20241008190413900](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008190413900.png)

![image-20241008190444293](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241008190444293.png)

希望灵活使用

# vector总结

## 1.begin() end() back()

![image-20241009132224507](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009132224507.png)

begin():返回指向 `vector` 首元素的迭代器。

若 `vector` 为空，则返回的迭代器将等于 [end()](mk:@MSITStore:C:\Users\30701\Downloads\C&C++帮助文档.chm::/chmhelp/cpp-container-vector-end.html) 

end():返回指向 `vector` 末元素后一元素的迭代器。

back():返回到容器中最后一个元素的引用。在空容器上调用 `back` 导致[未定义行为](mk:@MSITStore:C:\Users\30701\Downloads\C&C++帮助文档.chm::/chmhelp/cpp-language-ub.html)

![image-20241009132609795](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009132609795.png)

```
*c.begin()==*(c.begin())
```

## 2.性质

`vector` 是动态数组，它可以根据需要自动扩展容量。因此，你可以直接向 `vector` 中添加元素,不用再定义数组大小

![image-20241009140647312](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009140647312.png)

对于数组,数组名即是指针,因此可以这样写;

对于容器,名称没有含义,要用迭代器或者begin(),end()函数(返回的是指针)**左闭右开**

![image-20241009141025630](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009141025630.png)

![image-20241009143936670](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009143936670.png)

**find**()--algorithm库

![image-20241009144227478](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009144227478.png)

```
if (find(m.begin(), m.end(), "hello") == m.end())
        cout << "no" << endl;
```

**insert()**

![image-20241009145842179](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009145842179.png)

# stoi():string转换到int

![image-20241009182204537](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241009182204537.png)

# 刷题经验

**使用多重循环时记得注意是否要在每一层循环开始时重置变量**

**题目给的数据太大还用暴力循环，会超时**

# pair总结

![image-20241014121357316](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014121357316.png)

## 基本用法

![image-20241014121451731](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014121451731.png)

![image-20241014121547989](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014121547989.png)

![image-20241014121859207](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014121859207.png)

字典序排列

![image-20241014122008621](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014122008621.png)

![image-20241014122137398](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014122137398.png)

![image-20241014122216275](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014122216275.png)

![image-20241014123512145](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014123512145.png)

# 高精度运算

## 概念

![image-20241014153608145](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014153608145.png)

## 大整数相加

```c++
#include<iostream>
#include<string>
#include<vector>
using namespace std;

//1、高精度加法(大整数加法)
vector<int> add(vector<int>& A, vector<int>& B)
{
	//如果B更大，因为下面代码都是以第一个形参作为for结束条件，所以要让大的是第一个形参
	if (A.size() < B.size()) return add(B, A);

	vector<int> C;
	int t = 0; //用来判断是否进位
	//注意这里是逆序的数从前往后加的
	for (int i = 0; i < A.size(); ++i)
	{//for循环是以大的数来作为循环结束条件的
		t += A[i];//for循环以A为结束条件，这里不用格外判断
		if (i < B.size()) t += B[i];//因没以B为结束条件，故这里要格外判断是否可以加
		C.push_back(t % 10);//加出来的数要的是余数
		t /= 10; //判断是否有进位
	}
	if (t) C.push_back(t);//有可能最后加完还有进位
	 
	//第二种写法
	//这种写法不用格外判断最后是否有进位
	//for (int i = 0, t = 0; i < A.size()||t; ++i)
	//{
	//	if (i < A.size()) t += A[i];//因为有t作为条件，故这里要格外判断
	//	if (i < B.size()) t += B[i];//因没以B为结束条件，故这里要格外判断是否可以加
	//	C.push_back(t % 10);//加出来的数要的是余数
	//	t /= 10; //判断是否有进位
	//}
	 
	return C;

}

int main()
{
	string a, b;
	cin >> a >> b;
	vector<int> A, B;
	//把字符串逆序存入vector中，方便后续计算
	for (int i = a.size() - 1; i >= 0; --i) A.push_back(a[i] - '0');
	for (int i = b.size() - 1; i >= 0; --i) B.push_back(b[i] - '0');

	auto C = add(A, B);
	for (int i = C.size() - 1; i >= 0; --i) cout << C[i];
	cout << endl;
	 
	return 0;

}
```

## 大整数相乘

![image-20241014153859408](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014153859408.png)

```c++
#include <iostream>
#include <vector>
using namespace std;
const int N=100010;
vector<int> mul(vector<int> &A,int b)
{
    vector<int> C;
    int t=0;
    for(int i = 0;i<A.size()||t;i++)
    {
        if(i<A.size())t+=A[i]*b;
        C.push_back(t%10);
        t/=10;
    }
    while(C.size()>1 && C.back()==0)C.pop_back();//去掉前导0
    return C;
}
int main()
{
    string a;//a是一个比较大的数
    int b;//b比较小
```

​    

```c++
cin>>a>>b;

vector<int> A;
for(int i=a.size()-1;i>=0;i--)A.push_back(a[i]-'0');

auto C = mul(A,b);

for(int i = C.size()-1;i>=0;i--)printf("%d",C[i]);

return 0;
}
```
## 大整数相减

```c++
#include<iostream>
#include<string>
#include<vector>
using namespace std;

//比较哪个数大，注意这里的数是从倒序存的，故后面的才是高位
bool cmp(vector<int>& A, vector<int>& B)
{
	if (A.size() != B.size()) return A.size() > B.size();
	for (int i = A.size() - 1; i >= 0; --i)
		if (A[i] != B[i]) 
			return A[i] > B[i];//不等从高位开始比

	return true;//相等

}

vector<int> sub(vector<int>& A, vector<int>& B)
{//利用cmp函数比较，使大的数一定是A，与for循环代码相符

	vector<int> C;
	int t = 0;//判断借位
	for (int i = 0; i < A.size(); ++i)
	{
		t = A[i] - t;//每次都会减掉借位
		if (i < B.size()) t -= B[i];
		//关于(t+10)%10（t是减出来的数）
		//t若为正数(但<=9)其=t%10+10%10=t
		//t若为负数，正好可以借位+10然后取余数即可
		C.push_back((t + 10) % 10);
		if (t >= 0) t = 0;
		else t = 1; //<0肯定有借位了
	}
	//因为两个数相减会导致有多余的0出现，故去除前导0
	//size()>1是因为可能真的相减出现0，这种0不算前导0
	while (C.size() > 1 && C.back() == 0) C.pop_back();
	 
	return C;

}

int main()
{
	string a, b;
	cin >> a >> b;
	vector<int> A, B;
	//把字符串逆序存入vector中，方便后续计算
	for (int i = a.size() - 1; i >= 0; --i) A.push_back(a[i] - '0');
	for (int i = b.size() - 1; i >= 0; --i) B.push_back(b[i] - '0');

	vector<int> C;
	if (cmp(A, B)) C = sub(A, B); //正数
	else cout << "-", C = sub(B, A); //负数
	 
	for (int i = C.size() - 1; i >= 0; --i) cout << C[i];
	cout << endl;
	 
	return 0;

}


```

![image-20241014154346536](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014154346536.png)

## 大整数除法

```c++
#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;

vector<int> div(vector<int>& A, int b, int& r)
{
	vector<int> C;
	for (int i = A.size() - 1; i >= 0; --i)
	{
		r = r * 10 + A[i];
		C.push_back(r / b);
		r %= b; //计算余数
	}
	//逆置:因为我们是正常求，但最后是倒着读的，且便于去除前导0
	reverse(C.begin(), C.end());
	while (C.size() > 1 && C.back() == 0) C.pop_back();

	return C;

}

int main()
{
	string a;
	int b;

	cin >> a >> b;
	 
	vector<int> A;
	for (int i = a.size() - 1; i >= 0; --i) A.push_back(a[i] - '0');
	 
	int r = 0; //余数
	auto C = div(A, b, r);
	 
	for (int i = C.size() - 1; i >= 0; --i) cout << C[i];
	cout << endl << r << endl;
	 
	return 0;

}
```

![image-20241014154603575](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014154603575.png)

![image-20241014154810059](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014154810059.png)

## 多个数累乘

```c++
#include <iostream>
#include <vector>

using namespace std;

// 高精度乘法函数，将大整数存储在vector<int>中，逐位处理乘法和进位
void multiply(vector<int>& result, int x) {
    int carry = 0;
    for (int i = 0; i < result.size(); ++i) {
        long long product = result[i] * 1LL * x + carry;
        result[i] = product % 10;  // 保留当前位
        carry = product / 10;  // 计算进位
    }
    while (carry > 0) {
        result.push_back(carry % 10);  // 处理剩余的进位
        carry /= 10;
    }
}

// 打印高精度结果
void printBigInt(const vector<int>& result) {
    for (int i = result.size() - 1; i >= 0; --i) {
        cout << result[i];
    }
    cout << endl;
}

int main() {
    int n;
    cout << "输入pair的数量: ";
    cin >> n;

    vector<pair<int, int>> a(n);
    cout << "输入每对pair的first和second (只使用first): " << endl;
    for (int i = 0; i < n; ++i) {
        cin >> a[i].first >> a[i].second;  // 只用a[i].first进行乘法
    }
    
    // 初始结果为1（高精度数1存储为vector<int>{1}）
    vector<int> result(1, 1);
    
    // 逐个乘以pair的first
    for (int i = 0; i < n; ++i) {
        multiply(result, a[i].first);
    }
    
    // 输出结果
    cout << "所有pair.first的高精度乘积是: ";
    printBigInt(result);
    
    return 0;

}
```

![image-20241014201458704](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241014201458704.png)

# 蓝桥

## 判断是否超时

![image-20241102144336543](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102144336543.png)

![image-20241102144830551](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102144830551.png)

n是数据范围，O(f(n))是时间复杂度，计算出的f(n)是一个10的指数，即机器操作次数

-10^9_+10^9:int型表示范围

10^18：long long 范围

### 总结：

尽量不要超过**10^7**.logn可看作10，2^10=10^3，2^100=10^31

n在10^3-10^31次方之间看作10

可以根据数据范围推出时间复杂度，进而得到解题方法

# 常用头文件

#include<cstring>:有memset();

#include<cstdio>:有scanf(),printf();

## scanf(),printf();cin,cout 区别

![image-20241102153405528](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102153405528.png)

机器执行次数

![image-20241102153606314](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102153606314.png)

快一倍

## 分析递归

所有递归可以转化为搜索树

斐波那契数列例子：

![image-20241102154600800](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102154600800.png)

![image-20241102155138245](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102155138245.png)



常见2的次方

![image-20241102155803011](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102155803011.png)

递归后要恢复现场（指数型递归）

![image-20241102173222448](C:\Users\30701\AppData\Roaming\Typora\typora-user-images\image-20241102173222448.png)

puts(""):输出回车

输出括号里的内容并自动加上回车

# 全局变量

如果定义为全局变量那么初始值为0

定义为局部变量初始值为随机值
